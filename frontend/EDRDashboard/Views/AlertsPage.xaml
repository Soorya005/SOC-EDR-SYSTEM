<Page
    x:Class="EDRDashboard.Views.AlertsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="Transparent">

    <Grid Padding="24" RowSpacing="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Spacing="4">
            <TextBlock
                Text="Alerts"
                Style="{StaticResource SecurityTitleStyle}"/>
            <TextBlock
                Text="Detected security events requiring analyst attention"
                Style="{StaticResource SecuritySubtitleStyle}"/>
        </StackPanel>

        <!-- Filters Bar -->
        <Grid Grid.Row="1" ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="180"/>
                <ColumnDefinition Width="180"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Search box -->
            <AutoSuggestBox
                x:Name="SearchBox"
                PlaceholderText="Search alerts..."
                QueryIcon="Find"
                TextChanged="SearchBox_TextChanged"
                VerticalAlignment="Center"/>

            <!-- Severity ComboBox -->
            <ComboBox
                x:Name="SeverityCombo"
                Grid.Column="1"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Center"
                SelectedIndex="0"
                SelectionChanged="SeverityCombo_SelectionChanged">
                <ComboBoxItem Content="All Severities"/>
                <ComboBoxItem Content="Critical"/>
                <ComboBoxItem Content="High"/>
                <ComboBoxItem Content="Medium"/>
                <ComboBoxItem Content="Low"/>
            </ComboBox>

            <!-- Status ComboBox -->
            <ComboBox
                x:Name="StatusCombo"
                Grid.Column="2"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Center"
                SelectedIndex="0"
                SelectionChanged="StatusCombo_SelectionChanged">
                <ComboBoxItem Content="All Statuses"/>
                <ComboBoxItem Content="New"/>
                <ComboBoxItem Content="Investigating"/>
                <ComboBoxItem Content="Resolved"/>
            </ComboBox>

            <!-- Refresh Button -->
            <Button
                Grid.Column="3"
                Style="{StaticResource SecurityButtonStyle}"
                VerticalAlignment="Center"
                Click="RefreshButton_Click"
                Padding="12,8">
                <StackPanel Orientation="Horizontal" Spacing="6">
                    <SymbolIcon Symbol="Refresh" Foreground="White"/>
                    <TextBlock Text="Refresh" FontSize="12"/>
                </StackPanel>
            </Button>
        </Grid>

        <!-- Alerts Table Panel -->
        <Border
            Grid.Row="2"
            Background="{StaticResource CardBackgroundBrush}"
            BorderBrush="{StaticResource CardBorderBrush}"
            BorderThickness="1"
            CornerRadius="4"
            Padding="20">

            <Grid RowSpacing="12">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Table Headers -->
                <Grid ColumnSpacing="12" Padding="12,0" Background="#161A22" Height="36">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="100"/>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition Width="150"/>
                        <ColumnDefinition Width="100"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="Severity" FontSize="11" FontWeight="Bold" Foreground="#6B7280" VerticalAlignment="Center"/>
                    <TextBlock Grid.Column="1" Text="Detection Rule" FontSize="11" FontWeight="Bold" Foreground="#6B7280" VerticalAlignment="Center"/>
                    <TextBlock Grid.Column="2" Text="MITRE Technique" FontSize="11" FontWeight="Bold" Foreground="#6B7280" VerticalAlignment="Center"/>
                    <TextBlock Grid.Column="3" Text="Host" FontSize="11" FontWeight="Bold" Foreground="#6B7280" VerticalAlignment="Center"/>
                    <TextBlock Grid.Column="4" Text="Status" FontSize="11" FontWeight="Bold" Foreground="#6B7280" VerticalAlignment="Center"/>
                    <TextBlock Grid.Column="5" Text="Created" FontSize="11" FontWeight="Bold" Foreground="#6B7280" VerticalAlignment="Center"/>
                    <TextBlock Grid.Column="6" Text="Actions" FontSize="11" FontWeight="Bold" Foreground="#6B7280" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                </Grid>

                <!-- Alert Rows -->
                <ListView
                    x:Name="AlertsList"
                    Grid.Row="1"
                    ItemsSource="{Binding Alerts}"
                    SelectionMode="Single"
                    SelectionChanged="AlertsList_SelectionChanged"
                    Padding="0"
                    Margin="0">
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                            <Setter Property="Padding" Value="0"/>
                            <Setter Property="Margin" Value="0,1"/>
                            <Setter Property="Background" Value="Transparent"/>
                        </Style>
                    </ListView.ItemContainerStyle>
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Border BorderBrush="{StaticResource CardBorderBrush}" BorderThickness="0,0,0,1" Padding="12,12">
                                <Grid ColumnSpacing="12">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="100"/>
                                        <ColumnDefinition Width="2*"/>
                                        <ColumnDefinition Width="120"/>
                                        <ColumnDefinition Width="120"/>
                                        <ColumnDefinition Width="120"/>
                                        <ColumnDefinition Width="150"/>
                                        <ColumnDefinition Width="100"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- Severity Badge -->
                                    <Border Background="Transparent" 
                                            BorderBrush="{Binding Severity, Converter={StaticResource SeverityToColorConverter}}" 
                                            Style="{StaticResource SeverityBadgeStyle}">
                                        <TextBlock Text="{Binding Severity}" 
                                                   Foreground="{Binding Severity, Converter={StaticResource SeverityToColorConverter}}" 
                                                   FontSize="10" 
                                                   FontWeight="Bold"/>
                                    </Border>

                                    <!-- Detection Rule -->
                                    <TextBlock Grid.Column="1" Text="{Binding Title}" Foreground="White" FontSize="13" VerticalAlignment="Center" FontWeight="SemiBold"/>

                                    <!-- MITRE Technique -->
                                    <TextBlock Grid.Column="2" Text="{Binding Technique}" Foreground="#9CA3AF" FontSize="13" VerticalAlignment="Center"/>

                                    <!-- Host -->
                                    <TextBlock Grid.Column="3" Text="{Binding Endpoint}" Foreground="#9CA3AF" FontSize="13" VerticalAlignment="Center"/>

                                    <!-- Status -->
                                    <TextBlock Grid.Column="4" Text="{Binding Status}" Foreground="#9CA3AF" FontSize="13" VerticalAlignment="Center"/>

                                    <!-- Created -->
                                    <TextBlock Grid.Column="5" Text="{Binding Time}" Foreground="#6B7280" FontSize="12" VerticalAlignment="Center"/>

                                    <!-- Actions button -->
                                    <Button Grid.Column="6" 
                                            Content="Investigate" 
                                            Style="{StaticResource PrimarySecurityButtonStyle}" 
                                            FontSize="11" 
                                            Padding="10,4"
                                            Click="InvestigateButton_Click"
                                            Tag="{Binding}"
                                            HorizontalAlignment="Center"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </Border>
    </Grid>
</Page>